import ClockHistory from "./IconClockHistory";
import Profile from "./IconProfile";
import Exit from "./IconExit";
import Filter from "./IconFilter";
import Home from "./IconHome";
import Trash from "./IconTrash";

/*
    Each icon used in the application is creacted as a component returning an svg element.
    Here all the components are brought together and returned as an Object.
*/
export const Icons = {ClockHistory, Profile, Exit, Filter, Home, Trash};